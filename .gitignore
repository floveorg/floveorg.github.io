# ─── Contenido flove ──────────────────────────────────────────────
# docs/ y context/ SÍ se versionan (decisión 2026-06-24). En este clon
# docs/ pesa ~1.9 MB; context/ no existe. Si reaparece la taxonomía
# pesada de FloveAll (~GB), ignórala con una regla concreta, no docs/ entero.

# ─── Salidas de build ─────────────────────────────────────────────
# /blog (salida Hugo) SÍ se versiona — es lo que sirve flove.org/blog/.
# Se ignoran solo los dirs de trabajo de Hugo dentro de blog-src/.
public/
blog-src/public/
blog-src/resources/
.hugo_build.lock

# ─── Lanzador local (se GENERA en build-flove-zip.sh, no se versiona) ──
# El paquete flove.zip lleva su propio start-flove.sh + .desktop portables.
# Estas copias en disco son tuyas, específicas de tu máquina (icono absoluto).
start-flove.sh
flove-localhost.desktop

# ─── Sistema / editores ───────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.log

# ─── Node (por si añades herramientas) ────────────────────────────
node_modules/
package-lock.json
package.json

# ─── Cloudinary uploader ──────────────────────────────────────────
.env.cloudinary
.cloudinary-progress.json
cloudinary-urls.json
upload-to-cloudinary.js

# ─── Cards locales (movidas de others/, no se publican) ───────────
# SVG/PDF/HTML de cards (flove, futbolia) guardadas solo en tu máquina.
images/cards/

# ─── Harness / worktrees de Claude Code ──────────────────────────
.claude/
