# flove — context map (for agents)

**Working directory:** Always `/home/kdeneon/Documents/flove` — never other flove folders (e.g. `flove-apk`).

This folder is the **flove** chapter of the context book. This file is **only a
map** — the actual standards are not here; they live in their own named files.
Open the one you need:

- **Mandatory frontend contract** → [`contract.md`](contract.md)
- **Frontend patterns catalogue (§13)** → [`frontend.md`](frontend.md)
- **Standards index / matrix** → [`README.md`](README.md)
- **Vocabulary matrix (the file /vocaby fills)** → [`flove-vocaby.html`](flove-vocaby.html) — the single HTML file the /vocaby skill updates & fills when it syncs an app's vocabulary (slot × labeler grid in its `vocab` object). Its **"Prompt for Claude" export** (`app.V#: [slot · labeler] value`, one item per row, generated by `vocabLines()`) is the reference for the proper prompt of the update task — use it as the format to match. Renamed 2026-08-03 from the old `flove-tiers-matrix.html`.
- **Per-app adoption checklist (§14)** → [`adoption.md`](adoption.md)
- **Read-me-first map** → [`overview.md`](overview.md)
- **Architecture / backend spec** → [`backend.md`](backend.md)
- **Philosophy / ethos** → [`worldview.md`](worldview.md)
- **Dev principles (Worthing yardstick)** → [`principles.md`](principles.md)
- **Implementation plans & conflicts** → [`debates/plans/index.md`](debates/plans/index.md)
- **Central distro standards** → [`central.md`](central.md)
- **Solo status & index** → [`solo/README.md`](solo/README.md) — Solo is the local-first distro; Central apps are the canonical spec

When Marc names a frontend pattern ("tier model", "compass", "topbar",
"surfaces", "forms-iframe", "i18n t-en", "export", "locking"…), open
`frontend.md §13.x` first. `ray-*`/`bot-*` are the **old** vocabulary
— use `labeler-*`/`wizard-*` (§13.7).

## Home-directory layout (naming standard)

The user's working dirs were renamed from `claude` to the neutral **agents** name:

- `~/.agents` — user-owned config (skills symlink → `development/standards/skills`), the analog of old `~/.claude`
- `~/agents` — the personal notes/credential dir (old `~/Claude`), incl. `.git-credentials`
- repo convention: `.agents/` and `AGENTS.md` (old `CLAUDE.md`)

The following **Claude Code runtime dirs are kept intentionally** — they are the
tool's own state, not user content, and must NOT be renamed, duplicated, or
overwritten by future agents:

- `~/.claude.json` (Claude Code CLI state)
- `~/.local/share/claude`, `~/.local/state/claude`
- `~/.cache/claude`, `~/.cache/claude-cli-nodejs`
- `~/.local/bin/claude` (installed binary)
- `~/.vscode/extensions/anthropic.claude-code-*`

Do not create parallel `.claude`-style config or skill dirs; everything new goes
under `~/.agents` / `~/agents` / repo `.agents`.
