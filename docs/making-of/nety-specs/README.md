# nety-specs — bundle

All artifacts from the nety / flovenet / appy design conversation (2026-06-18), plus copies
of the referenced app files for context.

## Produced here
- **`2026-06-18-nety-access-ecosystem-design.md`** — the design spec (Kinetic Mesh redesign,
  four-facet trust, invite web, MyNet/circles, permissions, stars/stages, donation directing,
  recovery; nety = lower / appy = upper; profily-mini ↔ profily).
- **`2026-06-18-nety-spec-vs-flovenet-oasis-comparison.md`** — spec ↔ flovenet ↔ oasis
  match/conflict, the resolved bridge (one keypair + GraphQL gateway), data-ownership map, and
  the §8/§9 adoption plan (copy oasis's matches, refine conflicts; flovenet-independent +
  oasis as an optional add-on).
- **`2026-06-18-nety-frontend-build.md`** — the implementation plan (11 frontend tasks).
- **`nety-spec-questionnaire.html`** — interactive questionnaire (A–L, 57 questions): reveal
  Marc's selections, save frozen HTML, copy a conflict-analysis prompt.

## Referenced app files (copies, prefixed `ref-`)
- `ref-nety.html` — current nety landing (the redesign target).
- `ref-profily.html` — the profile page (→ becomes `profily`; basic = `profily-mini`).
- `ref-sety.html` — puzzy settings page (athen-ia Wizard lives here).
- `ref-appy-index.html` — the apps gallery (`appy` upper-layer launcher).
- `ref-athenea-logos.html` — LowAI athenea brand reference (navy+gold) for `wizy.html`.

> Note: these `ref-` copies are snapshots for context; the live files stay under `apps/`.
> The spec/comparison/plan also exist in their canonical locations under
> `docs/superpowers/{specs,plans}/` and `docs/nety-spec-questionnaire.html`.
